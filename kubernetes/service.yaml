apiVersion: v1
kind: Service
metadata:
  name: mein-server-service
spec:
  type: NodePort
  selector:
    app: mein-server
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080